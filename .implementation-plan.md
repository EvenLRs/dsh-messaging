# dsh-messaging 0.2.0 静态化实施计划

立项日期：2026-08-25
目标：将 dsh-messaging 从「静态壳 + 动态双半区」改造为纯静态 host + client 插件，解决设置页每次重启需手动 Run 的问题。

依据：`docs/static-plugin-migration.md` 调研报告

---

## 阶段划分（按报告建议顺序）

### Phase 1: HTTP RPC 基础设施
**目标：** 为静态 client 准备同源 HTTP 通道，替换动态 `host.call` / `harness.handle`

- [x] 在当前 `dynamic/host.js` 添加 HTTP 路由（动态工厂无法拆 ESM，双栈挂在同一 apply 内）
- [x] 端点清单（来自报告 §4 全表）：
  - `GET  /__dsh-messaging/status`
  - `GET  /__dsh-messaging/config`
  - `POST /__dsh-messaging/config`
  - `POST /__dsh-messaging/reload`
  - `POST /__dsh-messaging/send`
  - `POST /__dsh-messaging/ilink/login/start`
  - `POST /__dsh-messaging/ilink/login/status`
  - `POST /__dsh-messaging/ilink/login/verify`
  - `POST /__dsh-messaging/ilink/login/cancel`
- [x] 鉴权：Origin 校验（只允许 `127.0.0.1` / `localhost` / `::1`，对齐 host-bridge）
- [x] 测试：`node test/http-routes.cjs` + `node test/host-smoke.cjs`
- [x] 暂时保留 `harness.handle` 双栈，便于对照

**交付物：** HTTP 路由实现 + 单元测试 + `docs/http-rpc-map.md`

---

### Phase 2: Host 静态化
**目标：** `lib/index.js` 吃掉 `dynamic/host.js` 业务逻辑，删除动态插件启动壳

#### 2.1 迁移业务逻辑
- [x] `dynamic/host.js` 去掉工厂包装 `return { name, inject, apply }`（业务迁入 `lib/index.js`；`dynamic/` 留到 Phase 4 删除）
- [x] 改为 ESM：`export const name = 'dsh-messaging'`
- [x] `export const inject = ['webServer', 'shell', 'fs', 'agents', 'timer', 'agentDefaultModel']`
- [x] `export async function apply(ctx) { ... }`
- [x] 保留启动壳中的 companion 同步逻辑
- [x] 保留 `~/.dsh-messaging/config.json` 预置逻辑
- [x] `CONFIG_ROOT_OVERRIDE` 改为 `homedir()`；测试可通过 `ctx.get('dshMessaging.root')` 覆盖，避免写真实 home
- [x] 所有 `harness.handle` 调用替换为 Phase 1 的 HTTP 路由注册

#### 2.2 工具注册
- [x] 本阶段只保留 HTTP UI RPC，移除 `harness.defineTool('messaging_status')`（agent 工具可在后续用静态 dsh-tools 加回）

#### 2.3 删除动态启动逻辑
- [x] 删除 `lib/index.js` 中的：
  - `agents.create({ sessionId: 'dsh-messaging-bootstrap' })`
  - `dynamicCordisRunner.define(...)`
  - `runner.runHostHalf(...)`
  - `runner.undefine(...)`
  - `ctx.interval` 轮询等待 `timer`

#### 2.4 验证
- [x] Host 随 DSH 启动自动激活（静态 loader apply；真机回归在 Phase 4）
- [x] 消息网关（webhook / 轮询）smoke：`node test/host-smoke.cjs`
- [x] `ctx.agents.create` 会话能正常 followup（非 bootstrap 会话；OneBot inbound smoke 覆盖）

**交付物：** 静态 host ESM + 删除动态启动壳

---

### Phase 3: Client 静态化
**目标：** `lib/client.js` 打包为 `__ModuleLoader__` bundle，设置页自动出现

#### 3.1 Client bundle 结构
- [x] 创建 `lib/client.js`（`window.__ModuleLoader__` factory）
- [x] `host.call('messaging_*')` 全部替换为 `rpc()` → `fetch('/__dsh-messaging/...', { credentials: 'same-origin' })`
- [x] 设置页注册：`ctx.slots.inject('settings.section', ...)`，保持 `id: 'messaging'` / `order: 25`
- [x] 保留 `tool.view.cordis` 状态面板（静态 slots 同样可注册）

#### 3.2 package.json 声明
- [x] 添加 `exports["./client"] = "./lib/client.js"`
- [x] 添加 `dsh.client.platform = web` + inject 包名边
- [x] `files` 纳入 `lib`（含 `lib/client.js`）；`dynamic/` 不再打包

#### 3.3 验证
- [x] `node --check lib/client.js` + `node test/client-smoke.cjs`（VM ModuleLoader；首渲只 GET config；Save 才 POST）
- [ ] 真机：DSH 启动后设置页自动出现（Phase 4）

**交付物：** 静态 client bundle + `dsh.client` 声明

---

### Phase 4: 清理与测试
**目标：** 删除动态遗留，更新测试，真机回归

#### 4.1 删除动态遗留
- [x] 删除 `dynamic/` 目录（host.js / client.js / manifest.json）
- [x] README / CHANGELOG 写明：Cordis 面板中旧的 `dshmsg-*` 动态实例需手动删除

#### 4.2 更新测试
- [x] `test/host-smoke.cjs` 改为 `import()` ESM host
- [x] Mock `ctx.webServer.register` / `ctx.shell` / `ctx.fs` / `ctx.agents`
- [x] 断言 HTTP 路由表，而非 `harness.handle`
- [x] `test/client-smoke.cjs` 改为 VM 跑 `__ModuleLoader__`
- [x] 断言 `settings.section` 注册、挂载只 GET config、Save 才 POST

#### 4.3 真机回归（阻塞发版）
- [x] 本机 `dsh plugin --profile web` 安装开发 tarball；`dump-config` 挂上 `id: dsh-messaging`，无 `dshmsg-*`
- [ ] GUI：DSH 重启后设置页自动出现（未启动 Web UI，见 `docs/phase4-regression.md`）
- [ ] GUI：OneBot 真收发 / 微信扫码 / 保存重载（smoke 已覆盖协议路径）

**交付物：** 更新的测试套件 + 真机回归报告

---

### Phase 5: 发版与集成
**目标：** 发布 0.2.0，DeepRein 集成

#### 5.1 版本准备
- [ ] 更新 `package.json` 版本至 `0.2.0`
- [ ] 更新 `CHANGELOG.md`：
  - **Breaking:** 设置页现在随 DSH 启动自动出现，无需手动 Run
  - **Breaking:** Cordis 面板中旧的 `dshmsg-*` 动态实例应手动删除（否则可能双开）
  - 迁移到纯静态插件架构
  - Client 通信改为同源 HTTP
- [ ] 更新 `README.md`：删除"Client 需手动 Run"的提示

#### 5.2 发布
- [ ] `npm publish` 到 npm registry
- [ ] Git tag `v0.2.0`

#### 5.3 DeepRein 集成
- [ ] `deeprein/scripts/bundle-backend.mjs` bump `dsh-messaging@0.2.0`
- [ ] 本地联调：改 `local: join(root, '..', 'dsh-messaging')` 验证
- [ ] 提交前改回 `spec: 'dsh-messaging@0.2.0'`
- [ ] 走 Coder → Reviewer 流程
- [ ] CI 构建新 Tauri 包

**交付物：** npm 包 `dsh-messaging@0.2.0` + DeepRein 集成 commit

---

## 风险缓解措施（对应报告 §4）

| 风险 | 缓解措施 |
|---|---|
| Host 失去 VM 沙箱 | 保持现有 `pwshQuote` / 参数数组；HTTP RPC Origin 校验；不回退 webhook 鉴权 |
| `host.call` 漏迁 | Phase 1 列全表；client-smoke 覆盖每个按钮；真机回归 |
| `ctx.agents.create` 会话模型 | Phase 2 验证 followup；必要时显式 `sessionId` 前缀 |
| Client 包格式错误 | 对照 `update-checker` / `dshmarket`；`platform` 必须 `"web"` |
| 旧动态实例双开 | Phase 4 检查；0.2.0 changelog 提示用户手动删除 |
| Slack 无签名 / Discord 缺 `ws` | 已知问题，不阻塞静态化；可顺手记入 0.2.0 |

---

## 时间估计（参考报告 §6）

| 阶段 | 估计工作量 |
|---|---|
| Phase 1: HTTP RPC | 1-2 天 |
| Phase 2: Host 静态化 | 1-2 天 |
| Phase 3: Client 静态化 | 2-3 天 |
| Phase 4: 清理与测试 | 2-3 天 |
| Phase 5: 发版与集成 | 0.5-1 天 |
| **总计** | **6-11 天** |

不计真机回归中发现的严重 bug 修复时间。

---

## 依赖与前置条件

- [x] 调研报告完成（`docs/static-plugin-migration.md`）
- [ ] 确认 `dsh-messaging` 仓库当前版本（`0.1.2`）
- [ ] 本地开发环境能跑 DSH（已有 `@deepseek-ai/dsh@0.1.0-rc.6`）
- [ ] DeepRein 能本地联调 `bundle-backend.mjs` 的 `local` 路径

---

## 下一步

等待分配具体实施任务。建议按 Phase 顺序逐步推进，每个 Phase 完成后进入 Coder → Reviewer 审计闭环。
